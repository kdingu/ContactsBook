

<div class="navbar navbar-expand-lg navbar-dark bg-dark">
    @Html.ActionLink("CB", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto" id="">
            <li class="nav-item">
                @Html.ActionLink("Contacts", "Contacts", "Book", null, new { @class = "nav-link" })
            </li>

            @if (User.IsInRole(ContactsBook.Models.RolesModel.Admin))
            {
                <li class="nav-item">
                    @Html.ActionLink("Users", "Users", "Book", null, new { @class = "nav-link" })
                </li>
            }
        </ul>
        @Html.Partial("_LoginPartial")
    </div>
</div>